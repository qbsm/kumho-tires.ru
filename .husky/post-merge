#!/usr/bin/env sh
# После каждого git pull смотрим, сможет ли веб-сервер писать в logs и cache: файлы,
# созданные деплоем от своего пользователя, регулярно приезжают без права записи у группы,
# и приём заявок отвечает 500 вместо честного ответа. Предупреждаем, но pull не роняем.
npm run check:permissions --silent || true
